<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title></title>
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bulma@1.0.0/css/bulma.min.css"
    />
  </head>
  <body>
    <!-- 
    
    This code below mean: include the hero view template and pass the object 
    {title:appConfig.title, description:appConfig.description}
    to the hero view template 

    <%- include ('hero', {title:appConfig.title, description:appConfig.description}); %>  
    -->

    <!-- Use prettier-ignore to preserve the formatting -->
    <!-- prettier-ignore -->
    <%- include ('hero', {title:appConfig.title, description:appConfig.description}); %>
    <!-- prettier-ignore -->
    <%- include ('content', {internetPackages: appConfig.internetPackages}); %>
    <!-- prettier-ignore -->
    <%- include ('footer', {}); %>
  </body>
</html>
